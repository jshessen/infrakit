# Reference -- https://docs.goauthentik.io/docs/install-config/configuration/

#### Identity Provider - Authentik
#
AUTHENTIK_IMAGE=ghcr.io/goauthentik/server  # Default: ${AUTHENTIK_IMAGE:-ghcr.io/goauthentik/server}
AUTHENTIK_TAG=2024.10.0                     # Default: ${AUTHENTIK_TAG:-2024.10.0}
AUTHENTIK_PORT_HTTP=9000                    # Default: ${AUTHENTIK_PORT_HTTP:-9000}:9000"
AUTHENTIK_PORT_HTTPS=9443                   # Default: ${AUTHENTIK_PORT_HTTPS:-9443}:9443"

#### Authentik - Security
#
# Generate secrets using the commands below, then save to secrets/ directory:
# echo "PG_PASS=$(openssl rand -base64 36 | tr -d '\n')" > secrets/authentik_db_password
# echo "AUTHENTIK_SECRET_KEY=$(openssl rand -base64 60 | tr -d '\n')" > secrets/authentik_secret_key
#
# Database configuration (optional - uses defaults if not set):
# PG_USER=authentik       # Default: POSTGRES_USER=${PG_USER:-authentik}
# PG_DB=authentik         # Default: POSTGRES_DB=${PG_DB:-authentik}
