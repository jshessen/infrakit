#### Container Management - Portainer
#
PORTAINER_TAG=latest                        # Default: ${PORTAINER_TAG:-latest}
PORTAINER_PORT=9000                         # Default: ${PORTAINER_PORT:-9000}:9000

# Agent configuration (if using agent mode)
PORTAINER_AGENT_PORT=9001                   # Default: ${PORTAINER_AGENT_PORT:-9001}:9001/tcp

# Docker volumes path (adjust for your system)
DOCKER_VOLUMES=/var/lib/docker/volumes
